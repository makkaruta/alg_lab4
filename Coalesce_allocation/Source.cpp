#include <iostream>
#include <locale.h>
#include "CA.h"
#include <vector>
using namespace std;

int main() {
	setlocale(LC_ALL, "RUS");
	/*CA allocator;
	allocator.init();
	allocator.show();
	int* a = (int*)allocator.alloc(30);
	allocator.show();
	int* b = (int*)allocator.alloc(120);
	allocator.show();
	int* c = (int*)allocator.alloc(248);
	allocator.show();
	int* d = (int*)allocator.alloc(4);
	allocator.show();
	allocator.free(c);
	allocator.show();
	int* e = (int*)allocator.alloc(42);
	allocator.show();
	allocator.free(b);
	allocator.show();
	allocator.free(d);
	allocator.show();
	int* f = (int*)allocator.alloc(300);
	allocator.show();
	int* g = (int*)allocator.alloc(260);
	allocator.show();*/
	return 0;
}